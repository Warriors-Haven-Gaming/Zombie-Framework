#include "BaseControls.hpp"

// Couldn't find a way to programmatically set the number of columns
// so I guess we have to hardcode this
class RscShareMoneyToolbox : RscToolbox {
    rows = 1;
    columns = 6;
};
